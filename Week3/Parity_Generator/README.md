Here i implemented a simple parity generator where it checks 8 bits and with serial input but its gathered in a register 
if there are even number of ones it outputs a zero 
whereas if there an odd number it outputs a one

The testbench is made to detect if it outputs a one or a zero

